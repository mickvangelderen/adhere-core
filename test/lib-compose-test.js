process.env.NODE_ENV = 'test';

var expect = require('chai').expect;

var compose = require('../lib/compose');

describe('lib/compose.js', function() {

	describe('pipe', function() {
		it('should combine transforms', function() {

		});
	});

});